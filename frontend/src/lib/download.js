/* ============================================================
   حفظُ ملفٍّ من المتصفّح.

   ═══ الخطأان اللذان عولجا هنا، وكلاهما يظهر على آيفون وحده ═══

   **العنوان كان يُبطَل فور النقر.** والتنزيل يبدأ بعد النقر لا معه، فقد
   يجد المتصفّحُ العنوانَ مُبطَلاً قبل أن يقرأ منه — فيخرج الزرُّ بلا ملفّ
   ولا خطأ ولا حركة.

   **والعنصر يُلحق بالمستند ويُنزع بعده.** و`click()` على عنصرٍ خارج
   الشجرة لا يتنقّل في WebKit. والأصل هنا كان يُلحق، وهذا يُبقيه كذلك
   صراحةً لئلّا يسقط في نسخةٍ لاحقة.

   وكلاهما يعمل في كروم على سطح المكتب، فيمرّ في كل تجربة إلّا التي
   يُشتكى منها.

   ═══ ولا مشاركة هنا ═══

   على آيفون يُسلَّم الملفّ لتطبيق ناف، وهو يعرضه في شاشةٍ فيها «مشاركة»
   و«إغلاق». فلا يُبنى في الويب زرُّ مشاركةٍ ثانٍ لما يملكه الغلاف.
   ============================================================ */

/** كم يبقى عنوانُ الكائن حيّاً بعد النقر — دقيقة تكفي أبطأ تنزيلٍ يبدأ. */
const REVOKE_AFTER_MS = 60_000;

/** يحفظ كائناً ملفّاً باسمه. */
export function saveBlob(blob, filename) {
  const url = URL.createObjectURL(blob);
  const a = document.createElement('a');
  a.href = url;
  a.download = filename;
  document.body.appendChild(a);
  a.click();
  a.remove();
  window.setTimeout(() => URL.revokeObjectURL(url), REVOKE_AFTER_MS);
}
