// ============================================================================
// مسار الاستقبال — يستورد مسار الحزمة ولا يعيد بناء شيء منه.
//
// الحزمة تطابق الحالة العابرة، وتبادل رمز العبور خادماً لخادم، وتتحقق من
// التوقيع و iss و aud و exp، ثم تفتح جلسة المنصة.
// ============================================================================

import { handleCallback } from 'naf-auth';
import { authConfig } from './config.js';

export const ssoCallback = (c) => handleCallback(c.req.raw, c.env, authConfig(c.env));
